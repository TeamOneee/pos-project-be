import { Module } from '@nestjs/common';

// ReportingProjection + query dashboard (dari outbox event, bukan query tabel mentah). Implementasi diisi bertahap.
@Module({})
export class ReportingModule {}
