global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: 'nestjs-api'
    metrics_path: '/api/v1/metrics'
    static_configs:
      - targets: ['api.railway.internal:3000']
